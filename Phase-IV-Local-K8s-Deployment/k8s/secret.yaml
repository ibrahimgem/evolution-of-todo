# Kubernetes Secret for sensitive configuration
# [Task]: T026
# [From]: specs/004-local-k8s-deployment/spec.md §US2, plan.md §DD-002
#
# WARNING: This file contains placeholder values.
# In production, use Kubernetes Secrets management or external secret stores.
# Do NOT commit actual secrets to version control.
#
# To encode values: echo -n 'your-value' | base64
# To decode values: echo 'encoded-value' | base64 --decode

apiVersion: v1
kind: Secret
metadata:
  name: todo-chatbot-secret
  labels:
    app: todo-chatbot
type: Opaque
data:
  # Database credentials (base64 encoded)
  # echo -n 'postgres' | base64 = cG9zdGdyZXM=
  DATABASE_USER: cG9zdGdyZXM=
  # echo -n 'changeme' | base64 = Y2hhbmdlbWU=
  DATABASE_PASSWORD: Y2hhbmdlbWU=

  # API keys (base64 encoded)
  # echo -n '' | base64 = (empty)
  # Set your actual OpenAI API key before deployment
  OPENAI_API_KEY: ""

  # JWT secret (base64 encoded)
  # echo -n 'your-jwt-secret-change-in-production' | base64
  JWT_SECRET: eW91ci1qd3Qtc2VjcmV0LWNoYW5nZS1pbi1wcm9kdWN0aW9u
