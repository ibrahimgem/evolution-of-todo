# Backend Kubernetes Service (ClusterIP for internal access only)
# [Task]: T024
# [From]: specs/004-local-k8s-deployment/spec.md §US2, plan.md §DD-003

apiVersion: v1
kind: Service
metadata:
  name: todo-chatbot-backend
  labels:
    app: todo-chatbot
    component: backend
spec:
  type: ClusterIP
  selector:
    app: todo-chatbot
    component: backend
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000
